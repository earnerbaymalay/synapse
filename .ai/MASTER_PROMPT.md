# LLM Neurosurgeon — Master Build Prompt

**How to use:** Copy everything between `<<<PROMPT START>>>` and `<<<PROMPT END>>>` and paste it as your first message into **Claude Opus 4.8 inside Claude Code** (recommendation justified in §A after the prompt). It is self-executing: it will interview you once (branding + 3 stack/config choices), then build autonomously with only 5 human checkpoints. To run it on a different model, use the porting protocol in §B.

---

<<<PROMPT START>>>

# MISSION: Build "LLM NEUROSURGEON" — the cross-platform AI-config unifier

You are the **Chief Surgeon**: lead architect and orchestrator of an autonomous engineering effort. You will design, plan, and build a complete, shippable product with minimal human interruption. You have permission to make all technical decisions not explicitly reserved for the human at the gates defined below. Do not ask questions outside gates; log them to `QUESTIONS.md` and proceed with the most defensible default.

If your environment supports subagents, skills, hooks, and headless invocation (Claude Code), use the Swarm Protocol in §3. If it does not (plain chat), simulate each subagent sequentially in-context and treat every gate as a normal user question. Never claim a capability you lack.

---

## §1 PRODUCT VISION (expanded, authoritative spec)

**Problem.** Power users of AI coding tools maintain the same knowledge many times: skills for Claude Code, GEMINI.md for Gemini CLI, AGENTS.md for Codex/OpenCode, .cursor/rules, .clinerules, CONVENTIONS.md for Aider, copilot-instructions.md, plus per-tool MCP server configs. Formats drift, skills go stale in some tools, and no single place shows what any given model "knows."

**Solution.** A local-first desktop app + CLI that performs "neurosurgery" on the user's machine: it scans for every AI tool's config, extracts skills/agents/rules/memory/prompts/MCP servers into ONE canonical, human-readable directory (**the Brain**), then projects that Brain back out to every tool via symlinks or generated files, kept in sync automatically. Result: every model is equally skilled; the user edits in one place, forever.

**Product pillars (all are in scope):**

1. **The Brain** — one central directory (default `~/AIBrain`, user-configurable), clearly laid out, git-backed.
2. **Universal Import** — adapters that scan and losslessly ingest configs from: Claude Code/Desktop, Gemini CLI, OpenAI Codex CLI, Cursor, Windsurf, Cline/Roo Code, Aider, Continue, GitHub Copilot instructions, Zed, OpenCode, VS Code MCP configs. (Verify each tool's *current* config paths/formats from official docs at build time — do not trust memorized paths.)
3. **Projection Engine** — per-tool output: symlink where the tool tolerates it; otherwise emit a generated file stamped `GENERATED BY <brand> — edit in the Brain`. Embrace the emerging `AGENTS.md` standard as a first-class projection target. Windows: directory junctions, hardlink or copy+watch fallback when symlink privileges are absent.
4. **Auto-Sync Daemon** — filesystem watcher + scheduled sweeps (launchd/systemd/Task Scheduler). Bidirectional: source-edited → re-import; Brain-edited → re-project. Both changed → three-way merge against last git snapshot; disjoint markdown merges auto-resolve, real conflicts enter a review queue in the GUI. Every sync = a git commit (the "Time Machine": browse, diff, roll back).
5. **Marketplace Import** — one-click import of skills/agents from: the official `anthropics/skills` GitHub repo, awesome-claude-code / awesome-ai-skills lists, Gemini CLI extensions & official docs examples, and any git URL or local folder. Show provenance, license, and a diff preview before install.
6. **MCP Hub** — browse/search plug-and-play MCP servers from the official MCP registry, Smithery, PulseMCP, mcp.so, and the Docker MCP catalog; install once, project the server config to every tool's MCP file; health-check (spawn + handshake) each server; secrets stored in the OS keychain and written to tool configs as env placeholders only.
7. **Vitals & Doctor** — dashboard showing the coverage matrix (tool × capability), drift/broken-link detection, and a "Doctor" diagnostic that explains and fixes issues ("Cursor lost 3 rules — reattach?"). This is the neurosurgeon persona: precise, calm, a little clinical.
8. **Safety by design** — dry-run is the default for the first merge; a full backup snapshot precedes any destructive op; import never follows symlinks outside scanned roots; imported skills are flagged as untrusted prompt/code (show source + checksum, require explicit enable); no telemetry; fully offline except explicit marketplace fetches.

**Non-goals (v1):** running models, editing chat histories, cloud sync, mobile.

**Success criteria:** fresh machine → onboarded Brain in <5 min; round-trip import/export is semantically lossless for all 12 adapters; a skill edited in the Brain appears in every linked tool within 5 s (watcher) or one schedule tick; app runs on macOS, Windows, Linux.

**Canonical Brain layout (build to this):**

```
AIBrain/
  skills/<slug>/        SKILL.md + skill.yaml (id, version, triggers, targets, source, sha256)
  agents/<slug>.md      canonical agent defs (frontmatter: tools, model hints, targets)
  rules/                global.md + scoped/<glob>.md   (the single source for all *.md rule files)
  memory/               MEMORY.md + topic files
  prompts/              reusable commands/templates
  mcp/servers/<id>.yaml (transport, command/url, env-placeholders, targets, health)
  .brain/               mappings.json (source↔canonical↔projection + hashes), state.json,
                        locks, logs/, backups/
  .git/                 full history
```

**Implementation status (updated as of the doc-accuracy audit, not part of the
original prompt):** `packages/core/src/model.rs` implements `skills/`,
`agents/`, and `mcp/servers/` — the `rules/`, `memory/`, and `prompts/`
directories above are still spec, not code; no adapter or projector writes
them today. Likewise §1.4's "Auto-Sync Daemon" is unbuilt: `packages/core`
ships `watcher.rs`/`scheduler.rs` as library modules, but no CLI command
wires them up — `synapse sync` always runs a single reconciliation pass and
exits (see `docs/ARCHITECTURE.md` and `docs/USER_GUIDE.md` for the same note).

---

## §2 OPERATING SYSTEM — file-state, loop, and communication protocol

All memory lives in files, never in conversation. Create at start and keep current:

- `PLAN.md` — every task as `- [ ] T<phase>.<n> <task> :: verify: <command/check>`; gates tagged `[GATE]`.
- `PROGRESS.md` — append-only; 3 lines max per completed task (what, evidence, next).
- `DECISIONS.md` — every non-trivial technical decision, one line + rationale.
- `DESIGN_PACK.md` — created in Phase 1; binding for all UI work.
- `QUESTIONS.md` — deferred non-blocking questions, surfaced only at gates.

**RALPH LOOP (primary execution mode from Phase 2 onward).** Outer loop (human or script runs from terminal): `while PLAN.md has unchecked non-GATE tasks: claude -p "$(cat RALPH_PROMPT.md)"`. You will write `RALPH_PROMPT.md` in Phase 1 with exactly this inner algorithm:

1. Read `PLAN.md` + last 20 lines of `PROGRESS.md` (+ `DESIGN_PACK.md` only if the task is UI).
2. Select the SINGLE highest-priority unchecked, unblocked task.
3. Implement it completely.
4. Run its `verify:` check. Fix-and-rerun up to 3 attempts; still red → mark `[BLOCKED]` with a one-line note and stop touching it.
5. `git commit` (conventional message), tick the checkbox, append PROGRESS entry. STOP.

Each iteration starts with fresh context — that, not summarization, is the core token saver. Guard: if the last 3 PROGRESS entries reference the same task, mark it BLOCKED and escalate to `QUESTIONS.md`. The loop must halt at `[GATE]` tasks and request human review.

**Milestone updates.** At each gate, and only at gates, post:

```
## MILESTONE <n> — <phase>
Shipped: …            Evidence: <test counts, screenshot paths, demo cmd>
Risks: …              Decision needed: <one question + recommended option>
```

---

## §3 SWARM PROTOCOL — subagents, models, token economy

Use subagents aggressively but purposefully. Standing roster (create as project agents in `.claude/agents/`):

| Agent | Model | Job | Return contract |
|---|---|---|---|
| `recon` | Haiku | Verify current config paths/format of ONE tool from official docs; write `docs/research/<tool>.md` | ≤120-word brief |
| `adapter-smith` ×N | Sonnet | Build+test ONE adapter in an isolated git worktree; N run in parallel | ≤150 words + file list |
| `ui-builder` | Sonnet | Implement one screen strictly from `DESIGN_PACK.md` | ≤150 words + screenshot path |
| `test-writer` | Sonnet | Golden fixtures, round-trip property tests | ≤100 words |
| `red-team` | Opus | Attack phase-exit claims: symlink escapes, path traversal, destructive-op guards, junk configs | pass/fail + repro steps |
| `doc-writer` | Haiku | README, user guide, in-app copy | ≤80 words |

Orchestrator (you, Opus) only plans, integrates, reviews, and gates — never grinds boilerplate.

**Token economy — hard rules:**

1. File-state over chat-state; fresh ralph iterations instead of long sessions.
2. Grep first, then `Read` with offset/limit; never re-read unchanged files; never `cat` whole trees.
3. Batch all independent tool calls in one block; zero narration between calls.
4. Model tiering: Opus plans/reviews, Sonnet builds, Haiku researches/documents.
5. Subagent returns are capped (table above); details go to files, not the transcript.
6. Keep `RALPH_PROMPT.md` and `CLAUDE.md` byte-stable across iterations (prompt-cache friendly); `CLAUDE.md` ≤60 lines.
7. Reuse templates/skills; scaffold with generators, not token-generated boilerplate.
8. `/compact` (or equivalent) at every phase boundary; parallelize only independent tasks.
9. One adapter = one worktree = one subagent; merge via PR-style review by red-team, not re-reading diffs in main context.
10. Screenshots and test logs are referenced by path, never inlined.

---

## §4 PHASED PLAN (execute in order; every phase ends with self-verification, then its gate if marked)

### PHASE 0 — Discovery & Design Elicitation → **GATE 0 (human)**

Steps: (0.1) Initialize repo + state files. (0.2) Dispatch `recon` ×12 in parallel — one per supported tool — to confirm live config formats. (0.3) Produce THREE complete brand identities (below), each with an SVG logo sketch and one HTML mock of the Dashboard using its palette. (0.4) Produce ASCII wireframes (below) for all 8 screens. (0.5) Present GATE 0 questions via AskUserQuestion (or numbered list): ① brand A/B/C, ② default theme dark/light, ③ stack confirm (recommended: **Tauri 2 — Rust core for fs/watcher/symlinks + React/TS/Tailwind UI + shared Rust CLI**; alternative: Electron/Node if JS-only preferred), ④ Brain location default.

**Brand candidates (render fully, then let the human pick):**

- **A. Cortex** — "Your models. One mind." Palette *Deep Synapse*: bg `#0B0E1A`, violet `#6C5CE7`, cyan accent `#00D4FF`, text `#E8E9F0`. Type: Space Grotesk / Inter / JetBrains Mono. Dark, neural, premium.
- **B. Synapse** — "Wire every AI to one brain." Palette *Surgical Steel*: bg `#F7F9FB`, teal `#16A085`, slate `#2C3E50`, alert coral `#FF6B6B`. Type: Inter / IBM Plex Mono. Light, clinical, trustworthy.
- **C. Cerebra** — "Equal skills for every model." Palette *Warm Clinic*: cream `#FAF6F0`, coral `#E17055`, charcoal `#2D3436`, sky `#74B9FF`. Type: Sora / Inter. Friendly, approachable.
- In all cases, "LLM Neurosurgeon" remains the Doctor-mode persona and marketing hook.

**Wireframes (8 screens; refine these, don't replace them):** 1 Onboarding wizard (Scan → Review found configs → Create Brain → Link tools), 2 Dashboard/Vitals (coverage matrix, sync status, alerts), 3 Library (skills/agents/rules browser + editor + target toggles), 4 Marketplace (source tabs, diff preview, provenance), 5 MCP Hub (installed / registry / health / secrets), 6 Sync Center (timeline, conflict queue, schedules), 7 Doctor (diagnostics + one-click fixes), 8 Settings.

```
┌ CORTEX ─────────────────────────────────────────────┐
│ ◉ Vitals   Library   Marketplace   MCP   Sync   Dr. │
│ ┌─ Brain health ─────────┐ ┌─ Coverage matrix ─────┐│
│ │ 47 skills · 12 agents  │ │        sk ag ru mcp   ││
│ │ 9 rules · 6 MCP        │ │ Claude  ●  ●  ●  ●    ││
│ │ Last sync 2m ● healthy │ │ Gemini  ●  ●  ●  ◐    ││
│ └────────────────────────┘ │ Cursor  ●  –  ●  ●    ││
│ ⚠ 1 conflict: rules/global.md   [Review] [Auto]     │
└──────────────────────────────────────────────────────┘
```

Self-verify: 12 recon briefs exist; 3 HTML mocks render; wireframes cover all 8 screens. **GATE 0:** human picks; write choices to `DECISIONS.md`.

### PHASE 1 — Design Pack → **GATE 1 (human, 60-second skim)**

Compile `DESIGN_PACK.md`: chosen name/logo/tagline; full token sheet (colors incl. semantic states, type scale, spacing 4-pt grid, radii, elevation, motion 150–250 ms ease-out); component inventory (buttons, cards, matrix cell, diff view, toast, modal, tree); voice & microcopy rules (clinical-calm, verbs first, no exclamation marks); accessibility floor (WCAG AA, full keyboard nav, reduced-motion); the 8 wireframes redrawn in the chosen brand. Also write `RALPH_PROMPT.md` and the full task breakdown into `PLAN.md` (every task with a `verify:`). Self-verify: every UI task in PLAN.md cites a DESIGN_PACK section. **GATE 1:** approve pack or request one revision round.

### PHASE 2 — Architecture & Scaffold

Monorepo: `apps/desktop` (Tauri 2: `src-tauri/` Rust + `ui/` React-TS-Tailwind), `packages/core` (Rust crate: scanner, canonical model, adapters, projector, sync engine — exposed to UI via Tauri commands and to `apps/cli` via lib), `packages/schema` (JSON Schema for skill.yaml/agent/mcp; generated TS types), `apps/cli` (clap; verbs: `scan, import, project, sync, doctor, snapshot, rollback`), `fixtures/` (golden config trees per tool), CI matrix macOS/Windows/Linux. Core sync logic:

```
on watch_event | tick:
  for each mapping: h_src=hash(source); h_brain=hash(canonical)
    src changed only   -> import(source) -> project(all other targets)
    brain changed only -> project(all targets)
    both changed       -> three_way_merge(base=last_snapshot)
                          disjoint-md ? auto : conflict_queue.push
  git.commit("sync: <summary>"); update mappings.json hashes
```

Self-verify: `cargo test` + `pnpm test` green on CI matrix; empty app launches on all 3 OSes; CLI `--help` complete.

### PHASE 3 — Core Engine: adapters + projection → **GATE 2 (human, safety-critical)**

Parallel `adapter-smith` swarm, one per tool, worktree-isolated. Each adapter implements `detect() / import() -> Canonical / project(Canonical) -> files` + golden fixtures + **round-trip property test: import→canonical→project ≡ semantic identity**. Projection engine: symlink-vs-generate policy table per tool/OS; `mappings.json` maintained; drift detector. Secrets found during import → keychain, placeholder left behind. Red-team pass: symlink-escape, path traversal, circular links, 10k-file configs, malformed YAML/JSON/TOML. Self-verify: 12/12 adapters round-trip green; red-team pass; coverage ≥80% on core. **GATE 2:** human runs `cli scan && cli import --dry-run` against their real machine and approves the printed migration report — nothing is written before this approval.

### PHASE 4 — Sync Daemon & Time Machine

Watcher (debounced), scheduler registration per OS, conflict queue API, git snapshot/rollback commands, crash-safe lock. Self-verify: kill-during-sync leaves repo clean; simultaneous-edit test produces exactly one queued conflict; rollback restores byte-identical state; soak test 1 h with scripted edits every 10 s → zero drift.

### PHASE 5 — GUI → **GATE 3 (human screenshot review)**

`ui-builder` implements the 8 screens strictly from DESIGN_PACK; every destructive action = preview-diff + confirm; onboarding wizard wraps Phase-3 dry-run flow. Self-verify: Playwright/tauri-driver e2e per screen; screenshot diff vs. pack tokens (colors/spacing) automated; axe-core AA pass; keyboard-only walkthrough scripted. **GATE 3:** human reviews the screenshot folder + 2-min demo video/gif.

### PHASE 6 — Marketplace & MCP Hub

Importers for the skill sources and MCP registries listed in §1 (feature-flag any registry whose API is unstable; verify endpoints live via recon first). Install pipeline: fetch → license+provenance card → diff preview → sandbox lint of any executable content → user-enable toggle default OFF. MCP: health-check handshake, secrets flow, project-to-all-tools. Self-verify: import 3 real skills from `anthropics/skills` and 2 registry MCP servers end-to-end on fixtures; checksums recorded; disable/enable round-trips.

### PHASE 7 — Hardening, Docs, Self-Update

Threat-model pass (fs writes, downloaded content, secrets) with red-team sign-off; Doctor rules library (≥12 fixable diagnoses); app auto-update (Tauri updater, signed); weekly source-freshness checks for marketplace indexes; `doc-writer`: README, user guide, adapter-authoring guide (so the community can add tools), CHANGELOG. Self-verify: docs build; `doctor` fixes every seeded fault in a corrupted fixture Brain; update channel dry-run works.

### PHASE 8 — Package & Release → **GATE 4 (final acceptance)**

Signed installers: `.dmg`, `.msi`, AppImage/`.deb`; CLI to Homebrew/Scoop formulae (draft); reproducible-build notes; smoke-test install matrix. **GATE 4:** human installs on their own machine, runs onboarding on real data, confirms the 5-minute success criterion. Ship v1.0.0.

---

## §5 CONTINUOUS VERIFICATION DOCTRINE

Every `PLAN.md` task carries an executable `verify:`. Phase exits additionally require: all tests green on the 3-OS CI matrix; no `[BLOCKED]` tasks unresolved in that phase; red-team sign-off where marked; `PROGRESS.md`/`DECISIONS.md` current. Dry-run before write, snapshot before destroy, checksum after copy — always.

## §6 DELIVERABLES MANIFEST

Repo (buildable on all 3 OSes) · installers ×3 · CLI binary · `DESIGN_PACK.md` · docs set · fixture library · test+coverage report · demo script/gif · signed git history mapping every commit to a PLAN task.

## §7 FIRST ACTION

Begin now: execute Phase 0, step 0.1. Do not summarize this prompt back. Your first output is the initialized state files and the dispatched recon swarm.

<<<PROMPT END>>>

---

## §A Target model & justification

**Target: Claude Opus 4.8 (`claude-opus-4-8`) running inside Claude Code**, with Sonnet 5 and Haiku 4.5 as subagent workers (the prompt's §3 table assigns them).

Why Opus 4.8 specifically: (1) this is long-horizon agentic engineering — hours of autonomous multi-file work where Opus-class models hold intent and drift least; (2) the plan depends on Claude Code-native machinery — subagents/swarm, skills, hooks, and headless `claude -p` for the ralph loop — and Opus 4.8 is the flagship tuned for exactly that harness; (3) the token strategy assumes an orchestrator/worker split: Opus only plans, reviews, and gates while cheaper models grind, which is both the fastest and the cheapest configuration. If your plan includes a Fable/Mythos-tier model, use it for Phase 0–1 (design judgment) and gate reviews, and keep Opus/Sonnet in the loop for build economics.

## §B Porting protocol — moving this prompt to another model

Paste the adapter prompt below into the **target** model, followed by the full master prompt:

```
You are an expert prompt engineer for [TARGET MODEL + HARNESS, e.g. "Gemini 3 Pro in Gemini CLI"].
Rewrite the prompt below so it runs natively on your platform. Requirements:
1. Preserve EVERY phase, task, verification check, HITL gate, state file name, and deliverable exactly.
2. Translate Claude-specific mechanics to your native equivalents:
   subagents/swarm → your delegation feature, or sequential simulation with the same return contracts;
   CLAUDE.md/skills → GEMINI.md / AGENTS.md / system instructions as appropriate;
   AskUserQuestion → plain numbered questions; ralph outer loop → your CLI's scripted/headless loop;
   MCP remains MCP.
3. Re-tune the token-economy rules to your context window and caching behavior.
4. Keep total length within ±15%. Output ONLY the rewritten prompt in a single code block.
```

Then verify the port: all 8 phases present, 5 gates present (0–4), state files named identically, §7 first action unchanged. Because all project memory lives in `PLAN.md`/`PROGRESS.md`/`DECISIONS.md`, you can even switch models mid-build — any model resumes from the files, not the chat.
